{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "PORT=3003 nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.8"
  }
}
